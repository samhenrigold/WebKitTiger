# Generator inputs carrying a conditional that disagrees between
# build/tiger-ui-port and build/tiger-web-port.
# Consumed by: tools/tiger-wire-remap.py --list <this file>
# 2 file(s) from 1 disagreeing flag(s).
# Run 2026-09-20 23:15 against wkcmake's reconfigured -port trees.
# 
# USE(CAIRO) is NOT one of the six structurally-unagreeable flags, so this
# is a configuration finding rather than remap work. The web process
# records display lists and never rasterises (NullImageBufferBackend), so
# it should not have Cairo at all -- see brief N1-A. Sent to wkcmake.
# 
# Once that is fixed this file should be empty again until the six wire
# flags actually diverge, which is when the remap is due.
#
Source/WebKit/Shared/WebCoreFont.serialization.in
Source/WebKit/Shared/cairo/WebCoreFontCairo.serialization.in
