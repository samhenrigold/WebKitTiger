diff --git a/clang/include/clang/Basic/DiagnosticGroups.td b/clang/include/clang/Basic/DiagnosticGroups.td
index 76f9addab..865888496 100644
--- a/clang/include/clang/Basic/DiagnosticGroups.td
+++ b/clang/include/clang/Basic/DiagnosticGroups.td
@@ -681,6 +681,7 @@ def ObjCRetainBlockProperty : DiagGroup<"objc-noncopy-retain-block-property">;
 def ObjCReadonlyPropertyHasSetter : DiagGroup<"objc-readonly-with-setter-property">;
 def ObjCInvalidIBOutletProperty : DiagGroup<"invalid-iboutlet">;
 def ObjCRootClass : DiagGroup<"objc-root-class">;
+def ObjCFragileExtensionIvars : DiagGroup<"objc-fragile-extension-ivars">;
 def ObjCPointerIntrospectPerformSelector : DiagGroup<"deprecated-objc-pointer-introspection-performSelector">;
 def ObjCPointerIntrospect : DiagGroup<"deprecated-objc-pointer-introspection", [ObjCPointerIntrospectPerformSelector]>;
 def ObjCMultipleMethodNames : DiagGroup<"objc-multiple-method-names">;
diff --git a/clang/include/clang/Basic/DiagnosticSemaKinds.td b/clang/include/clang/Basic/DiagnosticSemaKinds.td
index 2781ff81a..4223a7ff2 100644
--- a/clang/include/clang/Basic/DiagnosticSemaKinds.td
+++ b/clang/include/clang/Basic/DiagnosticSemaKinds.td
@@ -6418,6 +6418,11 @@ def ext_forward_ref_enum_def : Extension<
 
 def err_redefinition_of_enumerator : Error<"redefinition of enumerator %0">;
 def err_duplicate_member : Error<"duplicate member %0">;
+def warn_objc_fragile_extension_ivar : Warning<
+  "instance variable %0 is not declared in the primary class interface; on the "
+  "fragile Objective-C ABI its offset is invisible to subclasses compiled in "
+  "other translation units">,
+  InGroup<ObjCFragileExtensionIvars>;
 def err_misplaced_ivar : Error<
   "instance variables may not be placed in %select{categories|class extension}0">;
 def warn_ivars_in_interface : Warning<
diff --git a/clang/include/clang/Basic/LangOptions.def b/clang/include/clang/Basic/LangOptions.def
index 25f4575a5..f6d8fff64 100644
--- a/clang/include/clang/Basic/LangOptions.def
+++ b/clang/include/clang/Basic/LangOptions.def
@@ -318,6 +318,9 @@ LANGOPT(ObjCAutoRefCount , 1, 0, NotCompatible, "Objective-C automated reference
 LANGOPT(ObjCWeakRuntime     , 1, 0, NotCompatible, "__weak support in the ARC runtime")
 LANGOPT(ObjCWeak            , 1, 0, NotCompatible, "Objective-C __weak in ARC and MRC files")
 LANGOPT(ObjCSubscriptingLegacyRuntime         , 1, 0, NotCompatible, "Subscripting support in legacy ObjectiveC runtime")
+LANGOPT(ObjCFragileExtensionIvars, 1, 0, NotCompatible,
+        "allow instance variables in class extensions and @implementation "
+        "blocks, and default property synthesis, on the fragile ObjC ABI")
 LANGOPT(CompatibilityQualifiedIdBlockParamTypeChecking, 1, 0, Benign,
         "compatibility mode for type checking block parameters "
         "involving qualified id types")
diff --git a/clang/include/clang/Driver/Options.td b/clang/include/clang/Driver/Options.td
index 958d0d05a..cad380825 100644
--- a/clang/include/clang/Driver/Options.td
+++ b/clang/include/clang/Driver/Options.td
@@ -3593,6 +3593,12 @@ def fobjc_arc : Flag<["-"], "fobjc-arc">, Group<f_Group>,
   Visibility<[ClangOption, CC1Option]>,
   HelpText<"Synthesize retain and release calls for Objective-C pointers">;
 def fno_objc_arc : Flag<["-"], "fno-objc-arc">, Group<f_Group>;
+defm objc_fragile_extension_ivars : BoolFOption<"objc-fragile-extension-ivars",
+  LangOpts<"ObjCFragileExtensionIvars">, DefaultFalse,
+  PosFlag<SetTrue, [], [ClangOption, CC1Option],
+          "Allow instance variables in class extensions and @implementation blocks, "
+          "and default property synthesis, on the fragile Objective-C ABI">,
+  NegFlag<SetFalse, [], [ClangOption, CC1Option]>>;
 defm objc_encode_cxx_class_template_spec : BoolFOption<"objc-encode-cxx-class-template-spec",
   LangOpts<"EncodeCXXClassTemplateSpec">, DefaultFalse,
   PosFlag<SetTrue, [], [ClangOption, CC1Option],
diff --git a/clang/lib/Driver/ToolChains/Clang.cpp b/clang/lib/Driver/ToolChains/Clang.cpp
index 626133223..f436efef3 100644
--- a/clang/lib/Driver/ToolChains/Clang.cpp
+++ b/clang/lib/Driver/ToolChains/Clang.cpp
@@ -4203,7 +4203,10 @@ static void RenderObjCOptions(const ToolChain &TC, const Driver &D,
   // Allow -fno-objc-arr to trump -fobjc-arr/-fobjc-arc.
   // NOTE: This logic is duplicated in ToolChains.cpp.
   if (isObjCAutoRefCount(Args)) {
-    TC.CheckObjCARC();
+    // The toolchain check rejects deployment targets whose shipped runtime
+    // predates ARC; an explicit -fobjc-runtime= with native ARC overrides it.
+    if (!Runtime.hasNativeARC())
+      TC.CheckObjCARC();
 
     CmdArgs.push_back("-fobjc-arc");
 
@@ -7324,6 +7327,10 @@ void Clang::ConstructJob(Compilation &C, const JobAction &JA,
                    !Runtime.isNeXTFamily()))
     CmdArgs.push_back("-fobjc-encode-cxx-class-template-spec");
 
+  if (types::isObjC(Input.getType()))
+    Args.addOptInFlag(CmdArgs, options::OPT_fobjc_fragile_extension_ivars,
+                      options::OPT_fno_objc_fragile_extension_ivars);
+
   if (Args.hasFlag(options::OPT_fapplication_extension,
                    options::OPT_fno_application_extension, false))
     CmdArgs.push_back("-fapplication-extension");
diff --git a/clang/lib/Sema/SemaDeclObjC.cpp b/clang/lib/Sema/SemaDeclObjC.cpp
index bbd104909..28ae270d8 100644
--- a/clang/lib/Sema/SemaDeclObjC.cpp
+++ b/clang/lib/Sema/SemaDeclObjC.cpp
@@ -2174,8 +2174,9 @@ void SemaObjC::CheckImplementationIvars(ObjCImplementationDecl *ImpDecl,
     return;
 
   assert(ivars && "missing @implementation ivars");
-  if (getLangOpts().ObjCRuntime.isNonFragile()) {
-    if (ImpDecl->getSuperClass())
+  if (getLangOpts().ObjCRuntime.isNonFragile() ||
+      getLangOpts().ObjCFragileExtensionIvars) {
+    if (ImpDecl->getSuperClass() && getLangOpts().ObjCRuntime.isNonFragile())
       Diag(ImpDecl->getLocation(), diag::warn_on_superclass_use);
     for (unsigned i = 0; i < numIvars; i++) {
       ObjCIvarDecl* ImplIvar = ivars[i];
@@ -3015,7 +3016,8 @@ void SemaObjC::ImplMethodsVsClassMethods(Scope *S, ObjCImplDecl *IMPDecl,
   // of the property in the @implementation.
   if (const ObjCInterfaceDecl *IDecl = dyn_cast<ObjCInterfaceDecl>(CDecl)) {
     bool SynthesizeProperties = getLangOpts().ObjCDefaultSynthProperties &&
-                                getLangOpts().ObjCRuntime.isNonFragile() &&
+                                (getLangOpts().ObjCRuntime.isNonFragile() ||
+                                 getLangOpts().ObjCFragileExtensionIvars) &&
                                 !IDecl->isObjCRequiresPropertyDefs();
     DiagnoseUnimplementedProperties(S, IMPDecl, CDecl, SynthesizeProperties);
   }
@@ -5628,9 +5630,15 @@ Decl *SemaObjC::ActOnIvar(Scope *S, SourceLocation DeclStart, Declarator &D,
   if (!EnclosingDecl || EnclosingDecl->isInvalidDecl())
     return nullptr;
   ObjCContainerDecl *EnclosingContext;
+  // With -fobjc-fragile-extension-ivars we accept ivars outside the primary
+  // @interface on the fragile ABI too, and lay them out as if they had been
+  // declared there.
+  bool RejectExtraIvars =
+      getLangOpts().ObjCRuntime.isFragile() &&
+      !getLangOpts().ObjCFragileExtensionIvars;
   if (ObjCImplementationDecl *IMPDecl =
           dyn_cast<ObjCImplementationDecl>(EnclosingDecl)) {
-    if (getLangOpts().ObjCRuntime.isFragile()) {
+    if (RejectExtraIvars) {
       // Case of ivar declared in an implementation. Context is that of its
       // class.
       EnclosingContext = IMPDecl->getClassInterface();
@@ -5639,7 +5647,7 @@ Decl *SemaObjC::ActOnIvar(Scope *S, SourceLocation DeclStart, Declarator &D,
       EnclosingContext = EnclosingDecl;
   } else {
     if (ObjCCategoryDecl *CDecl = dyn_cast<ObjCCategoryDecl>(EnclosingDecl)) {
-      if (getLangOpts().ObjCRuntime.isFragile() || !CDecl->IsClassExtension()) {
+      if (RejectExtraIvars || !CDecl->IsClassExtension()) {
         Diag(Loc, diag::err_misplaced_ivar) << CDecl->IsClassExtension();
         return nullptr;
       }
@@ -5691,5 +5699,9 @@ Decl *SemaObjC::ActOnIvar(Scope *S, SourceLocation DeclStart, Declarator &D,
       isa<ObjCInterfaceDecl>(EnclosingDecl))
     Diag(Loc, diag::warn_ivars_in_interface);
 
+  if (getLangOpts().ObjCRuntime.isFragile() && !NewID->isInvalidDecl() &&
+      !isa<ObjCInterfaceDecl>(EnclosingDecl))
+    Diag(Loc, diag::warn_objc_fragile_extension_ivar) << NewID;
+
   return NewID;
 }
diff --git a/clang/lib/Sema/SemaObjCProperty.cpp b/clang/lib/Sema/SemaObjCProperty.cpp
index 9dbb1d28a..083c6701a 100644
--- a/clang/lib/Sema/SemaObjCProperty.cpp
+++ b/clang/lib/Sema/SemaObjCProperty.cpp
@@ -1332,9 +1332,13 @@ Decl *SemaObjC::ActOnPropertyImplDecl(
       ClassImpDecl->addDecl(Ivar);
       IDecl->makeDeclVisibleInContext(Ivar);
 
-      if (getLangOpts().ObjCRuntime.isFragile())
-        Diag(PropertyDiagLoc, diag::err_missing_property_ivar_decl)
-            << PropertyId;
+      if (getLangOpts().ObjCRuntime.isFragile()) {
+        if (getLangOpts().ObjCFragileExtensionIvars)
+          Diag(PropertyDiagLoc, diag::warn_objc_fragile_extension_ivar) << Ivar;
+        else
+          Diag(PropertyDiagLoc, diag::err_missing_property_ivar_decl)
+              << PropertyId;
+      }
       // Note! I deliberately want it to fall thru so, we have a
       // a property implementation and to avoid future warnings.
     } else if (getLangOpts().ObjCRuntime.isNonFragile() &&
@@ -1560,7 +1564,8 @@ Decl *SemaObjC::ActOnPropertyImplDecl(
     }
     IC->addPropertyImplementation(PIDecl);
     if (getLangOpts().ObjCDefaultSynthProperties &&
-        getLangOpts().ObjCRuntime.isNonFragile() &&
+        (getLangOpts().ObjCRuntime.isNonFragile() ||
+         getLangOpts().ObjCFragileExtensionIvars) &&
         !IDecl->isObjCRequiresPropertyDefs()) {
       // Diagnose if an ivar was lazily synthesdized due to a previous
       // use and if 1) property is @dynamic or 2) property is synthesized
@@ -1972,7 +1977,8 @@ void SemaObjC::DefaultSynthesizeProperties(Scope *S, ObjCImplDecl *IMPDecl,
 void SemaObjC::DefaultSynthesizeProperties(Scope *S, Decl *D,
                                            SourceLocation AtEnd) {
   if (!getLangOpts().ObjCDefaultSynthProperties ||
-      getLangOpts().ObjCRuntime.isFragile())
+      (getLangOpts().ObjCRuntime.isFragile() &&
+       !getLangOpts().ObjCFragileExtensionIvars))
     return;
   ObjCImplementationDecl *IC=dyn_cast_or_null<ObjCImplementationDecl>(D);
   if (!IC)
